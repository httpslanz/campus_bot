{
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "pip install --no-cache-dir -r requirements-production.txt && python manage.py migrate && python manage.py collectstatic --noinput && gunicorn campusbot.wsgi --bind 0.0.0.0:$PORT",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}